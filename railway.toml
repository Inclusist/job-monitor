[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# Removed: Use Dockerfile CMD (Gunicorn) instead of Flask dev server
# startCommand = "python app.py"
healthcheckPath = "/"
healthcheckTimeout = 600
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
